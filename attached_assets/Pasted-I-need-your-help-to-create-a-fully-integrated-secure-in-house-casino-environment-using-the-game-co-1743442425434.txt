I need your help to create a fully integrated, secure, in-house casino environment using the game code from this GitHub repository: [https://github.com/Casino-Group/The-Casino-Project](https://github.com/Casino-Group/The-Casino-Project). This should include a backend API and a basic website.

Here's a breakdown of what I need:

**1. Backend API (using Python Flask or FastAPI):**

* **Clone the GitHub repository:** Clone [https://github.com/Casino-Group/The-Casino-Project](https://github.com/Casino-Group/The-Casino-Project) into this Replit project.
* **Robust API Structure:** Design a more comprehensive API to manage the games. Consider the following endpoints (you can adjust these based on the specific games in the repository):
    * `/api/games`: Returns a list of available games.
    * `/api/game/{game_name}/start`: Initializes a new game session and returns a unique game ID and the initial game state.
    * `/api/game/{game_id}/play`: Accepts player actions (e.g., move, bet) and updates the game state. It should return the new game state and any relevant results (win/loss, etc.). The request should include the action data (e.g., in JSON format).
    * `/api/game/{game_id}/state`: Retrieves the current state of a specific game session.
* **Game Logic Integration:** Fully integrate the game logic from the cloned repository into these API endpoints. This will involve importing the necessary game classes/functions, managing game instances, and handling game flow within the API routes.
* **Basic Session Management (In-Memory for now):** Implement a basic way to track active game sessions, perhaps using a dictionary where the key is the `game_id` and the value is the game instance and its state.
* **Error Handling:** Include basic error handling for invalid requests, game not found, etc.

**2. Frontend Website (using HTML, CSS, and JavaScript):**

* **Basic Website Structure:** Create a simple HTML structure with a navigation bar and a main content area.
* **"Juegos Populares" Section:** Within the main content area, create a dedicated section labeled "Juegos Populares".
* **Dynamic Game Listing:** Use JavaScript to fetch the list of available games from the `/api/games` endpoint and dynamically display them in the "Juegos Populares" section. Each game should have a button or link to start playing.
* **Game Interaction:** When a user clicks on a game, use JavaScript to:
    * Call the `/api/game/{game_name}/start` endpoint to initiate a new game.
    * Handle user input and send actions to the `/api/game/{game_id}/play` endpoint.
    * Receive updates from the API (game state, results) and dynamically update the user interface to reflect the game's progress.
    * Periodically or on demand, call the `/api/game/{game_id}/state` endpoint to get the latest game state.
* **Simple Styling:** Include basic CSS to make the website presentable.

**3. Integration:**

* Ensure that the frontend website can communicate with the backend API running within the same Replit environment. You'll likely need to handle CORS (Cross-Origin Resource Sharing) if the frontend and backend are served on different ports (though Replit often handles this).
* The frontend should clearly display the game interface and provide ways for the user to interact with the game through the API.

**The goal is to have a functional website within Replit where users can see a list of available games in the "Juegos Populares" section, start a game, and play it by interacting with the backend API. All game logic and state management should happen on the server-side within the API.**

Please provide the file structure (likely separate folders for the backend and frontend) and the initial code for both the Flask/FastAPI application and the basic website (HTML, CSS, and JavaScript). Aim for a basic but functional implementation that demonstrates the full integration.
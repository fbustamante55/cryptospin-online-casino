**Objetivo:** Crear rutas API para soportar una variedad de juegos de tragamonedas personalizados, siguiendo una estructura similar a la del juego de Crash que ya tenemos.

**Por favor, ayúdame a definir la estructura inicial de estas rutas API y cómo podríamos gestionar diferentes configuraciones de tragamonedas:**

1.  **Ruta para Obtener la Configuración de un Slot Específico (`/api/slots/config/{slot_id}`):**
    * Describe cómo podríamos diseñar esta ruta `GET` para que el frontend pueda obtener la configuración de un juego de tragamonedas específico identificado por `{slot_id}`.
    * ¿Qué tipo de información debería devolver esta ruta en formato JSON? Piensa en los parámetros necesarios para renderizar el slot en el frontend (por ejemplo, número de carretes, símbolos por carrete, tabla de pagos, líneas de pago posibles, etc.).
    * ¿Cómo podríamos almacenar y gestionar las configuraciones de diferentes slots en nuestro backend (podría ser similar al almacenamiento local que usamos para Crash, o tal vez una estructura más organizada)?

2.  **Ruta para Realizar un Giro (`/api/slots/spin/{slot_id}`):**
    * Describe cómo podríamos diseñar esta ruta `POST` para que el frontend pueda solicitar un giro en un juego de tragamonedas específico (`{slot_id}`). Esta ruta también debería recibir la cantidad apostada por el usuario.
    * ¿Qué lógica principal debería implementar esta ruta en el backend? (Generación aleatoria de resultados de los carretes basada en la configuración del slot, determinación de si hay ganancias según la tabla de pagos y las líneas activas, cálculo del monto ganado).
    * ¿Qué tipo de información debería devolver esta ruta en formato JSON? (Resultado de los carretes, monto ganado, quizás el nuevo saldo del usuario si la gestión de la billetera está integrada aquí).

3.  **Gestión de la Configuración de los Slots:**
    * ¿Qué estrategias podríamos usar para definir y almacenar las configuraciones de nuestros diferentes juegos de tragamonedas personalizados en el backend? (Por ejemplo, usar diccionarios de Python con estructuras predefinidas, leer configuraciones desde archivos JSON, etc.). Proporciona un ejemplo de cómo podríamos definir la configuración para un slot simple de 3 carretes con algunos símbolos y una tabla de pagos básica.

**Por favor, proporciona las ideas y la estructura inicial para estas rutas API, así como sugerencias sobre cómo podríamos gestionar las configuraciones de los diferentes slots en nuestro backend.** No es necesario implementar la lógica completa en este paso, solo definir la arquitectura y las estructuras de datos iniciales.
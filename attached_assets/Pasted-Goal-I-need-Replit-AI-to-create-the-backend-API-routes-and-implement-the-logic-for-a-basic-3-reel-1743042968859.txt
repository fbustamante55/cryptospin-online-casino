Goal:** I need Replit AI to create the backend API routes and implement the logic for a basic 3-reel slot machine within my current Replit project. This API should have an endpoint that my frontend can call to "spin" the slots and receive the outcome (the symbols on the reels and any winnings).

**Please perform the following actions:**

1.  **Create a new Python file named `main.py`** in the root directory of my Replit project (or suggest a location if you think it should be elsewhere, explaining why).
2.  **In `main.py`, set up a basic web server using the Flask framework.** If Flask is not already installed, please include the necessary installation command.
3.  **Define an API route `/api/slots/spin` that accepts POST requests.**
4.  **Implement the core logic for a 3-reel slot machine within this route.** This logic should include:
    * A predefined set of symbols (e.g., "cherry", "lemon", "orange", "plum", "bell", "bar", "seven").
    * Assign different probabilities or weights to these symbols, making some more likely to appear than others. Please provide an example of these weights.
    * A mechanism to randomly select a symbol for each of the three reels based on these probabilities.
    * A basic payout structure defining winning combinations and their corresponding multipliers (e.g., three "cherry" symbols pay out 10x the bet, three "bar" symbols pay out 100x, etc.). Include a few example payout rules.
    * The route should accept an optional `bet` amount in the JSON request body (default to 1 if not provided).
    * The route should return a JSON response containing:
        * An array or list representing the symbols that landed on each of the three reels.
        * The total winnings for that spin (0 if no winning combination).
5.  **Include the necessary Flask code to run the development server** when the `main.py` file is executed. The server should run on a common port (e.g., 5000).
6.  **If a `.replit` file does not exist in my project, please create one and configure it to run the `main.py` file using `python main.py`.** If a `.replit` file exists, please suggest how the `run` command might need to be updated to execute the Flask server in `main.py`.

**Assumptions:**

* Assume a basic 3-reel slot machine.
* Use Python and the Flask framework.
* Create a simple payout structure for demonstration purposes.

**Output:** Please provide the complete `main.py` file content and the content of the `.replit` file (if created or modified). Also, provide the Flask installation command if needed."

**How to Use This Prompt:**

1.  Open your Replit project.
2.  Open the "AI" sidebar.
3.  Paste this entire prompt into the AI chat input.
4.  Send the prompt to Replit AI.

Replit AI should then generate the `main.py` file with the Flask API and slot machine logic, as well as configure your `.replit` file to run it. You can then run your Repl, and the `/api/slots/spin` endpoint will be active. You can test it using tools like Postman or by creating a simple frontend in your Replit project.